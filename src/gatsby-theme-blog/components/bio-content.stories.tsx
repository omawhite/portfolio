
import BioContent from './bio-content'

export default {
  title: 'Components/bio-content',
  component: BioContent,
}

const Template = (args) => <BioContent/>

export const Default = Template.bind({})
